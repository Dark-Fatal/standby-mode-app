<resources>
    <!-- Thème de base (sera utilisé par MainActivity) -->
    <style name="Theme.StandByMode" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:colorBackground">@android:color/black</item>
    </style>

    <!-- Thème pour l'horloge plein écran -->
    <style name="Theme.StandByMode.FullScreen" parent="Theme.StandByMode">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>